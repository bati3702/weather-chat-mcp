# 🌦️ Weather Israel MCP Server (Playwright)

פרויקט זה מציע שרת MCP (Model Context Protocol) מותאם אישית המאפשר לכל מודל שפה (LLM) לשלוט בדפדפן באופן אוטומטי לחלוטין ולשלוף מידע בזמן אמת על מזג האוויר בישראל מאתר [Weather2day](https://www.weather2day.co.il/forecast).

הפרויקט עושה שימוש ב-**Playwright** לצורך ניווט ואוטומציה חכמה בדפדפן, ובמנגנון **RAG (Retrieval-Augmented Generation)** כדי להעביר את המידע ישירות לתוך שיחת הצ'אט.

---

## 🎯 מטרת הפרויקט
* פיתוח שרת MCP מותאם אישית לחשיפת פונקציות (Tools) ל-LLM.
* ביצוע אוטומציה מלאה ואינטראקציה חכמה עם דפדפן (ללא מגע יד אדם).
* פתרון בעיות סנכרון ו-Flakiness בדפדפן באמצעות המתנה אקטיבית לשינויי תוכן בדף.
* שליפת התוכן של עמוד התחזית והזרקתו בחזרה לקונטקסט של המודל כדי לאפשר מענה מקיף ומעובד בתוך הצ'אט.

---

## 🛠️ טכנולוגיות בהן נעשה שימוש
* **Python**
* **FastMCP (MCP SDK)** - הספרייה הרשמית של Anthropic להגדרת פרוטוקול MCP.
* **Playwright (Python Async API)** - ספריית אוטומציה ושליטה בדפדפן Chromium.

---

## 🚀 איך להריץ את הפרויקט?

### 1. התקנת תלויות וסנכרון הסביבה
ודאי שאת נמצאת בתיקיית הפרויקט והריצי:
```bash
uv sync
2. התקנת דפדפן Chromium עבור Playwrightהריצי את הפקודה הבאה כדי להוריד ולהתקין את מנוע הדפדפן המתאים ל-Playwright:Bashuv run playwright install chromium
3. הגדרת משתני סביבה (מפתח API)צרי קובץ בשם .env בתיקיית השורש של הפרויקט, והוסיפי בתוכו את מפתח ה-OpenAI שלך בצורה הבאה:קטע קודOPENAI_API_KEY=your_actual_api_key_here
(הקובץ .env כבר מוגדר בתוך ה-.gitignore כך שהוא לעולם לא יעלה ל-GitHub הציבורי).4. הפעלת ה-Host (הצ'אט בטרמינל)הריצי את מנהל השיחה:Bashuv run host.py
🧩 הכלים החשופים בשרת (Tools)שם הכליתפקיד ומנגנון פעולהopen_weather_forecast_israelפותח דפדפן Chromium במצב ממוקסם ומנווט ישירות לעמוד הראשי של אתר התחזית.enter_weather_forecast_city_israelמקבל שם עיר, מנקה את שדה החיפוש ומקליד אותה תו-אחר-תו (עם השהיה קלה) כדי להפעיל בהצלחה את מנגנון ה-Autocomplete של האתר.select_weather_forecast_city_israelבוחר באופן אוטומטי בפריט הראשון ברשימת הערים המוצעות, מקליק עליו וממתין לטעינת הדף הייעודי של אותה העיר.get_forecast_content_israelסורק את הדף, מחלץ את הטקסט הנקי של התחזית, מנקה שורות ריקות ורווחים מיותרים, ומחזיר את המידע ישירות ל-LLM.
